name: Release

on:
  push:
    tags:
      - 'v*'

env:
  DOTNET_VERSION: '8.0.x'
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build-windows:
    name: 建置 Windows 安裝包
    runs-on: windows-latest
    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 設定 .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: 取得版本號
        id: get-version
        shell: bash
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: 還原相依套件
        run: dotnet restore

      - name: 執行測試
        run: dotnet test --configuration Release --verbosity normal

      - name: 發布應用程式
        run: dotnet publish src/TableSpec.Desktop -c Release -r win-x64 --self-contained -o publish

      - name: 建立 Velopack 安裝包
        run: |
          dotnet tool install -g vpk
          vpk pack -u TableSpec -v ${{ steps.get-version.outputs.version }} -p publish -e TableSpec.Desktop.exe --packTitle "TableSpec" -i src/TableSpec.Desktop/Assets/avalonia-logo.ico -o Releases

      - name: 上傳安裝包
        uses: actions/upload-artifact@v4
        with:
          name: windows-installer
          path: Releases/*
          retention-days: 30

  build-macos:
    name: 建置 macOS 安裝包
    runs-on: macos-latest
    strategy:
      matrix:
        runtime: [osx-arm64, osx-x64]
        include:
          - runtime: osx-arm64
            arch: arm64
          - runtime: osx-x64
            arch: x64
    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 設定 .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: 取得版本號
        id: get-version
        shell: bash
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: 還原相依套件
        run: dotnet restore

      - name: 執行測試
        run: dotnet test --configuration Release --verbosity normal

      - name: 發布應用程式
        run: dotnet publish src/TableSpec.Desktop -c Release -r ${{ matrix.runtime }} --self-contained -o publish

      - name: 建立 macOS 壓縮包
        run: |
          mkdir -p Releases
          cd publish
          zip -r ../Releases/TableSpec-${{ steps.get-version.outputs.version }}-${{ matrix.runtime }}.zip .
          cd ..
          ls -la Releases/

      - name: 上傳安裝包
        uses: actions/upload-artifact@v4
        with:
          name: macos-installer-${{ matrix.arch }}
          path: Releases/*
          retention-days: 30

  build-linux:
    name: 建置 Linux 安裝包
    runs-on: ubuntu-latest
    steps:
      - name: 檢出程式碼
        uses: actions/checkout@v4

      - name: 設定 .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: 取得版本號
        id: get-version
        shell: bash
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: 還原相依套件
        run: dotnet restore

      - name: 執行測試
        run: dotnet test --configuration Release --verbosity normal

      - name: 發布應用程式
        run: dotnet publish src/TableSpec.Desktop -c Release -r linux-x64 --self-contained -o publish

      - name: 建立 Velopack 安裝包
        run: |
          dotnet tool install -g vpk
          vpk pack -u TableSpec -v ${{ steps.get-version.outputs.version }} -p publish -e TableSpec.Desktop --packTitle "TableSpec" -i src/TableSpec.Desktop/Assets/TableSpec.png -o Releases

      - name: 上傳安裝包
        uses: actions/upload-artifact@v4
        with:
          name: linux-installer
          path: Releases/*
          retention-days: 30

  create-release:
    name: 建立 GitHub Release
    needs: [build-windows, build-macos, build-linux]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 下載所有安裝包
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 整理發布檔案
        run: |
          mkdir -p release-files
          cp artifacts/windows-installer/* release-files/ 2>/dev/null || true
          cp artifacts/macos-installer-arm64/* release-files/ 2>/dev/null || true
          cp artifacts/macos-installer-x64/* release-files/ 2>/dev/null || true
          cp artifacts/linux-installer/* release-files/ 2>/dev/null || true
          ls -la release-files/

      - name: 建立 Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-files/*
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, '-') }}
